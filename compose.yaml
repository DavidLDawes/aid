services:
  node:
    image: "node:24"
    user: "node"
    working_dir: /home/node/app
    environment:
      - NODE_ENV=production
    volumes:
      - ./:/home/node/app
    expose:
      - "4173"
    ports: # use if it is necessary to expose the container to the host machine
      - "4173:4173"
    command: "npm start"